---
name: tech-gap-checker
description: 基础设施盘点——在用户描述完项目想法后、写任何代码前，扫描八个维度，输出完整的"零件清单"，标记用户未提及的隐形需求。自动触发：用户描述要构建的软件（网页/应用/游戏/工具）且包含具体功能时，必须在写第一行代码前自动调用。触发关键词：做、建、开发、写一个、项目、app、website、应用、网页、游戏、工具、帮我做、我想要一个。
---

# 基础设施盘点（Tech Gap Checker）

## ⚠️ 自动调用规则

你必须在以下情况**自动调用本 skill，无需用户手动触发**：

1. 用户描述了一个新的软件项目（网页/应用/游戏/工具/小程序）
2. 用户描述了具体功能需求（如"用户可以提交内容""需要区分不同角色""数据要保存"等）
3. 用户让 AI 开始写代码实现某个想法

**调用时机：在写第一行代码之前。** 如果用户已经让你开始写代码了，先暂停，说"我先扫描一下技术盲区"，跑完本扫描再继续。

**不需要调用的场景：** 纯修改已有代码、Bug 修复、讨论想法但还没确定要做、纯技术问答。

---

## 你的职责

用户描述的是"用户看到什么、做什么"，但可能不知道背后需要什么技术零件来支撑。

你的任务：**把那些用户没说、但项目运行必须有的技术需求找出来。** 不假设用户懂任何技术。

## 第零步：项目类型识别与隐形需求加载

在做八维度扫描之前，先判断项目属于哪种类型，并自动加载对应的"隐形需求清单"。这些是该类型项目几乎必然需要、但用户通常不会主动提到的需求。

将隐形需求清单的每一项，作为额外的 ⚠️ 盲区候选，叠加进八维度扫描的对应维度里一并输出。

---

### 类型识别规则

根据用户描述，匹配以下类型（可多选）：

- **多人游戏类**：提到"一起玩""聚会""游戏""回合""胜负""牌""骰子""房间"
- **内容创作/社区类**：提到"发帖""评论""点赞""关注""内容""社区""广场"
- **工具/效率类**：提到"生成""转换""计算""分析""导出""模板""自动化"
- **电商/交易类**：提到"购买""下单""支付""商品""库存""售卖"
- **预约/活动类**：提到"预约""报名""活动""日历""时间段""名额"
- **仪表盘/数据类**：提到"统计""图表""报表""监控""数据看板"
- **教育/课程类**：提到"课程""学习""作业""考试""进度""证书"
- **AI 功能类**：提到"AI""生成""智能""GPT""对话""识别"

---

### 各类型隐形需求清单

#### 🎮 多人游戏类

**房间与会话**
- 房间创建机制：谁来创建？用链接邀请还是输入房间号？
- 房间容量上限：最多几人？超出怎么处理？
- 旁观者模式：是否允许不参与游戏的人围观？
- 房间过期机制：没人用的房间多久自动清除？

**游戏状态机**（这是最容易漏的）
- 等待中（房间创建，等人加入）
- 游戏进行中（不允许新人加入）
- 游戏结束（展示结果，可以重开）
- 每个状态之间的转换条件是什么？谁能触发？

**角色与权限**
- 是否有主持人/房主角色？
- 房主能做什么：踢人、开始游戏、重置游戏？
- 房主离开了游戏怎么办？权限自动转移还是游戏结束？

**异常处理**
- 玩家中途断线：等待重连还是直接移除？
- 玩家超时不操作：自动跳过还是整局暂停？
- 所有人都离开：房间立即销毁还是保留一段时间？

**游戏结束**
- 胜负结果如何展示？
- 是否需要保存历史战绩？
- 重新开始时：同一批人重开，还是需要重新建房？

---

#### 📝 内容创作/社区类

**内容生命周期**
- 草稿保存：用户写到一半，关掉页面，内容还在吗？
- 内容编辑：发布后还能改吗？改了要标注"已编辑"吗？
- 内容删除：删了对其他人还可见吗？软删除还是硬删除？
- 内容举报：被举报的内容谁来审核？

**互动机制**
- 点赞/收藏是否需要登录？游客可以点吗？
- 评论是否支持回复评论（多层嵌套）？
- @提及功能：提到某人会给他发通知吗？
- 内容分享：分享出去的链接，未登录用户能看到什么？

**内容加载**
- 内容多了怎么处理：分页还是无限滚动？
- 排序方式：按时间？按热度？用户可以切换吗？
- 搜索功能：支持搜索内容/用户吗？

**媒体处理**
- 图片上传后是否需要压缩？原图存储成本高
- 是否需要支持视频？视频存储和播放成本极高
- 用户头像：存在哪？限制尺寸吗？

---

#### 🛠️ 工具/效率类

**输入与输出**
- 用户的输入数据需要保存吗？下次打开还想用吗？
- 生成结果需要导出吗？导出成什么格式（PDF/Excel/图片）？
- 是否支持批量处理？一次处理多个文件？

**历史记录**
- 是否需要查看之前的使用记录？
- 历史记录保存多久？占存储空间
- 不同设备之间历史记录是否同步？（需要登录）

**分享与协作**
- 生成结果是否可以分享给他人？通过链接？
- 分享的链接是否永久有效？
- 他人打开分享链接能做什么：只看，还是可以基于此继续编辑？

**使用限制**
- 是否需要限制免费用户的使用次数？
- 高频调用（尤其是 AI API）成本怎么控制？

---

#### 🛒 电商/交易类

**订单流程**
- 加入购物车 → 确认订单 → 支付 → 支付成功/失败 → 发货 → 确认收货：每个环节的状态存在哪？
- 支付超时未付款：订单自动取消，库存还原
- 支付成功但网络中断：用户没看到成功页，怎么处理重复支付？

**库存管理**
- 库存是否实时同步？两个人同时抢最后一件怎么办？
- 是否需要预留库存（加入购物车时就锁定）？

**退款与售后**
- 退款流程：谁发起、谁审核、多久到账？
- 退货：需要填写退货原因吗？谁承担运费？

**价格与优惠**
- 是否有优惠券/折扣码？如何防止重复使用？
- 价格是否会变动？历史订单按下单时的价格算还是当前价格？

---

#### 📅 预约/活动类

**时间与名额**
- 同一时间段是否有人数上限？
- 超额预约：排队等候还是直接拒绝？
- 预约后可以取消吗？取消截止时间是什么时候？

**通知提醒**
- 预约成功后发确认通知（邮件/短信）？
- 活动前多久发提醒？
- 活动取消或变更时，如何通知已报名的人？

**重复预约**
- 同一人是否可以预约多个时间段？
- 是否防止同一人重复预约同一场次？

---

#### 📊 仪表盘/数据类

**数据来源**
- 数据从哪里来？手动录入、API 对接、还是文件上传？
- 数据更新频率：实时、定时刷新、还是手动刷新？

**权限隔离**
- 不同用户看到的数据范围一样吗？
- 是否有"只能看自己数据"的权限控制？

**数据导出**
- 图表是否可以导出为图片或 PDF？
- 原始数据是否可以导出为 Excel？

**历史数据**
- 数据保存多久？历史趋势需要存多长时间的数据？

---

#### 🎓 教育/课程类

**学习进度**
- 用户的学习进度存在哪？换设备还能继续吗？
- 视频课：记录看到第几秒，下次从断点继续？
- 章节是否需要解锁机制（完成前置才能看下一章）？

**作业与考试**
- 提交作业后还能修改吗？
- 考试是否有时间限制？关掉页面重新进来计时继续还是重置？
- 防作弊：是否需要限制切换标签页？

**证书与成就**
- 完成课程后自动生成证书吗？证书存在哪？可以分享吗？

---

#### 🤖 AI 功能类

**成本控制**（这是最容易忽视的）
- 每次调用 AI API 都有成本，用户无限调用吗？
- 是否需要按用户限制每日调用次数？
- 是否需要缓存相同问题的结果，避免重复计费？

**响应时间**
- AI 响应可能需要 5-30 秒，这段时间页面怎么显示？
- 是否需要流式输出（像 ChatGPT 那样逐字显示）？
- 超时了怎么处理？

**内容安全**
- 用户输入的内容是否需要过滤（防止 prompt injection）？
- AI 生成的内容是否需要审核后才展示？

**数据隐私**
- 用户输入的内容会发给第三方 AI 服务，用户知情吗？
- 是否需要在隐私政策里说明？

---

### 输出要求

识别完类型后，在输出的"📋 技术零件清单"里，新增一行：

    | 项目类型 | 已识别 | 多人游戏类（已加载对应隐形需求清单） |

然后将隐形需求清单里被触发的项，合并进对应维度的 ⚠️ 盲区输出里，不单独列出，避免输出过长。

---

## 扫描框架

一个完整 web 应用的技术栈，拆开来看就是这些层：

**前端交互 → 数据层 → 用户层 → 通信层 → 部署层 → 外部服务 → 安全 → 多端适配**

这八层是项目运行必备的基础设施，也是非技术人最容易漏掉的部分。

更进阶的维度（性能监控、异常追踪、自动化测试、SEO、无障碍访问）在项目规模化后才需要——本 Skill 聚焦 MVP 阶段，那八层覆盖了 90% 的盲区。

## 扫描流程

按以下八个维度逐一扫描，每个维度输出：
- ✅ 用户已提到的（标记为"已覆盖"）
- ⚠️ 用户没提但可能需要的（标记为"盲区"）
- ❌ 本项目不需要的（标记为"不适用"，并说明为什么）

---

### 一、多用户交互

检查项目是否需要多人参与：

| 问题 | 盲区触发条件 |
|------|------------|
| 多个用户是否需要各自操作？还是同屏看一个人操作？ | 用户说"大家一起"但没指定方式 |
| 一个人的操作是否需要立刻出现在另一个人的屏幕上？ | 提到"协作""投票""实时"等 |
| 是否需要创建房间、加入房间、邀请机制？ | 提到"多人""组队""房间" |
| 不同用户看到的内容是否不同？（如管理员 vs 普通用户） | 提到"后台""权限""审核" |
| 没有网络时是否还需要基本功能？ | 提到"随时随地""离线" |

**关键判断：多人 + 各自设备 = 必须有后端做状态同步。**

---

### 二、数据存储

检查数据持久化需求：

| 问题 | 盲区触发条件 |
|------|------------|
| 用户产生的内容是否需要持久化？刷新/关闭浏览器后还在吗？ | 任何有用户输入/提交的场景 |
| 是否需要保存用户信息、历史记录、设置偏好？ | 提到"个人主页""历史""收藏" |
| 是否需要上传图片/音频/文件？ | 提到"上传""图片""头像""附件" |
| 数据之间是否有关联？（如用户→文章→评论） | 提到"评论""回复""关联" |

**关键判断：用户产生内容 + 需要下次看到 = 需要数据库。**

---

### 三、用户系统

检查身份与权限需求：

| 问题 | 盲区触发条件 |
|------|------------|
| 如何区分不同用户？昵称？邮箱注册？第三方登录？ | 提到"用户""自己的""我的" |
| 是否有人拥有特殊权限？谁能删内容？谁能管理？ | 提到"管理员""审核""发布" |
| 如何防止冒名、重复注册、恶意操作？ | 提到"公开""任何人都能" |

**关键判断：需要知道"谁做了什么" = 必须有用户标识机制。**

---

### 四、实时通信

检查是否需要服务端推送：

| 问题 | 盲区触发条件 |
|------|------------|
| 用户需要收到实时通知吗？（新消息、状态变化） | 提到"通知""提醒""实时" |
| 多人同时在线时，状态如何同步？ | 提到"在线""协作""多人同时" |
| 用户切到后台再回来，状态还在吗？ | 应用内有复杂流程/状态 |

**关键判断：等所有人操作完再出结果 = 需要后端计数+推送。**

---

### 五、部署与托管

检查上线需求：

| 问题 | 盲区触发条件 |
|------|------------|
| 前端托管在哪里？ | 任何要给别人用的项目 |
| 如果有后端，部署在哪里？ | 前面扫描出需要后端 |
| 是否需要自定义域名？ | 提到"正式""上线""发布" |
| 是否有第三方 API Key 需要安全存储？ | 提到"对接""API""调用" |
| 预估同时使用人数，免费层够不够？ | 提到"很多人""公开""上线后" |

**关键判断：让别人能访问 = 必须部署。**

---

### 六、第三方服务

检查外部依赖：

| 问题 | 盲区触发条件 |
|------|------------|
| 是否需要数据库服务？（Supabase / Firebase 等） | 前面扫描出需要数据存储 |
| 是否需要支付？（收款、打赏、订阅） | 提到"付费""购买""打赏""会员" |
| 是否需要发送邮件或推送通知？ | 提到"通知""验证""提醒" |
| 是否需要调用外部 AI API？ | 提到"AI""智能""生成" |
| 是否需要统计/埋点？ | 提到"数据""分析""统计" |

**关键判断：需要外部能力 = 需要第三方服务。**

---

### 七、安全与边界

检查安全风险：

| 问题 | 盲区触发条件 |
|------|------------|
| 用户输入的内容是否需要过滤（XSS、SQL 注入）？ | 任何用户可输入的场景 |
| 是否需要防止恶意高频请求？ | 提到"公开""不需要登录" |
| 用户数据是否需要隔离？敏感信息如何处理？ | 提到"个人信息""密码""支付" |
| 用户产生的内容是否需要敏感词过滤？ | 提到"社区""评论""发布" |

**关键判断：接受用户输入 = 必须做输入校验。**

---

### 八、移动端与体验

检查多端适配：

| 问题 | 盲区触发条件 |
|------|------------|
| 是否需要在手机上正常显示和操作？ | 提到"手机""移动端""随时随地" |
| 是否需要添加到主屏幕、离线使用？ | 提到"像 App 一样""快捷方式" |
| 是否需要触屏手势支持？ | 提到"滑动""拖拽""长按" |
| 是否需要横竖屏适配？ | 提到"横屏""全屏""游戏" |
| 是否需要分享/邀请功能？ | 提到"邀请""分享""发给朋友" |

**关键判断：手机上用 = 必须考虑响应式布局。**

---

## 输出格式

扫描完成后，输出三部分：

### 📋 技术零件清单

用表格列出每个维度结论：

```
| 维度 | 状态 | 说明 |
|------|------|------|
| 多用户交互 | ⚠️ 盲区 | 各自手机操作需要后端同步 |
| 数据存储 | ✅ 已覆盖 | 用户提到了数据库 |
| 部署托管 | ⚠️ 盲区 | 没说前端放哪儿 |
| 支付系统 | ❌ 不适用 | 当前阶段不需要 |
```

### 🔑 本次最关键盲区（Top 3）

按影响程度排序，每个一句话说明为什么重要、用什么方案解决。

### 🛠️ 推荐技术选型

给出具体技术栈建议，标注免费额度上限。优先推荐免费方案。

---

## 硬规则

1. **必须在写代码前运行。** 如果用户已经让你开始写代码了，先暂停，跑完本扫描再继续。
2. **八个维度逐一扫描，不可跳过。** 每个维度必须输出明确判断（✅ / ⚠️ / ❌）。
3. **判断"不适用"必须写理由。** 不能只写"❌ 不适用"，必须写"当前阶段不需要，因为……"
4. **盲区标注要具体。** 不能写"可能需要后端"，要写"需要 Supabase Realtime，因为多人操作需要状态同步"。
5. **推荐方案要可落地。** 给出具体服务名和免费额度，不推荐模糊方案。
6. **不假设用户懂技术。** 用日常语言解释每个技术选择，不堆术语。
