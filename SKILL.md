---
name: tech-gap-checker
description: 技术盲区扫描——在用户描述完项目想法后、写任何代码前，扫描八个维度，输出完整的"零件清单"，标记用户未提及的隐形需求。自动触发条件：用户描述了要构建的软件（网页/应用/游戏/工具）且包含具体功能需求时，你必须在写第一行代码前自动调用本skill。触发关键词包括但不限于：做、建、开发、写一个、项目、app、website、应用、网页、游戏、工具、帮我做、我想要一个。
---

# 技术盲区扫描器（Tech Gap Checker）

## 触发时机
仅手动触发。用户需明确说"跑 tech-gap-checker"或"扫描技术盲区"。

## 你的职责
你假设用户不懂技术——她描述的是"用户看到什么、做什么"，但不知道背后需要什么零件来支撑。你的任务是把那些**她没说、但她需要**的技术零件找出来。

## 扫描流程

按以下八个维度逐一扫描，每个维度输出：
- ✅ 用户已提到的（标记为"已覆盖"）
- ⚠️ 用户没提但可能需要的（标记为"盲区"）
- ❌ 本项目不需要的（标记为"不适用"，并说明为什么）

---

### 一、多用户交互

| 项目 | 问题 |
|------|------|
| 多人协作 | 多个用户是否需要同时操作/查看同一内容？ |
| 实时同步 | 一个人的操作是否需要立刻出现在另一个人的屏幕上？ |
| 房间/组队 | 用户是否需要创建房间、加入房间、邀请好友？ |
| 角色区分 | 不同用户看到的内容是否不同（如主持人 vs 参与者）？ |
| 离线可用 | 没有网络时是否还需要基本功能？ |

**典型盲区：** 用户说"大家一起玩"，但没说"需要后端做状态同步"。多人+各自设备 = 必须有后端。

---

### 二、数据存储

| 项目 | 问题 |
|------|------|
| 用户数据 | 是否需要保存用户信息、历史记录、游戏进度？ |
| 内容数据 | 用户产生的内容（故事、投票、分数）是否需要持久化？ |
| 文件存储 | 是否需要上传图片/音频/文件？ |
| 数据关系 | 数据之间是否有关联（如用户→房间→游戏记录）？ |

**典型盲区：** 用户说"投完票出结果"，但没说"投票数据要存哪儿"。浏览器刷新就没了。

---

### 三、用户系统

| 项目 | 问题 |
|------|------|
| 身份识别 | 如何区分不同用户？昵称？登录？匿名？ |
| 权限控制 | 谁有权创建房间、开始游戏、删除内容？ |
| 安全验证 | 是否需要防止刷票、恶意输入、垃圾内容？ |

**典型盲区：** 用户说"每个人输入自己的名字"，但没说"两个人输入同一个名字怎么办"。

---

### 四、实时通信

| 项目 | 问题 |
|------|------|
| 消息推送 | 是否需要服务器主动通知用户（而不是用户刷新页面）？ |
| 状态同步 | 游戏状态（轮到谁了、谁投了票）如何同步给所有人？ |
| 连接保活 | 用户切到别的 App 再回来，状态还在吗？ |

**典型盲区：** 用户说"等所有人都投完票出结果"，但没说"怎么知道所有人都投完了"。这需要后端计数+推送。

---

### 五、部署与托管

| 项目 | 问题 |
|------|------|
| 前端托管 | 网页部署在哪里？（Vercel / GitHub Pages / Cloudflare Pages） |
| 后端托管 | 如果有后端，部署在哪里？（Supabase / Railway / VPS） |
| 域名 | 是否需要自定义域名？ |
| 环境变量 | 是否有第三方 API Key 需要安全存储？ |
| 并发承载 | 预估同时使用人数，免费层够不够？ |

**典型盲区：** 用户说"做个网页"，但没说"网页放哪儿让别人能访问"。

---

### 六、第三方服务

| 项目 | 问题 |
|------|------|
| 数据库服务 | 是否需要 Supabase / Firebase / PlanetScale？ |
| 支付 | 是否需要接入支付（收款、打赏、订阅）？ |
| 邮件/通知 | 是否需要发送邮件或推送通知？ |
| AI 能力 | 是否需要调用 AI API？ |
| 分析统计 | 是否需要统计访问量、用户行为？ |
| CDN / 图床 | 是否需要托管静态资源？ |

**典型盲区：** 用户说"发布到平台上卖"，但没说"平台之外还需要数据库存用户购买记录"。

---

### 七、安全与边界

| 项目 | 问题 |
|------|------|
| 输入校验 | 用户输入的内容是否需要过滤（XSS、SQL 注入）？ |
| 频率限制 | 是否需要防止用户疯狂点击/刷请求？ |
| 数据隐私 | 用户的游戏数据是否需要加密或隔离？ |
| 内容审核 | 用户产生的内容是否需要过滤敏感词？ |

**典型盲区：** 用户说"大家自由输入故事"，但没说"有人输入恶意代码怎么办"。

---

### 八、移动端与体验

| 项目 | 问题 |
|------|------|
| 响应式 | 是否需要在手机上正常显示和操作？ |
| PWA | 是否需要"添加到主屏幕"、离线使用？ |
| 触屏适配 | 拖拽、长按、手势是否需要特殊处理？ |
| 横竖屏 | 是否需要强制横屏或竖屏？ |
| 分享 | 是否需要"复制链接邀请好友"的功能？ |

**典型盲区：** 用户说"手机上打开链接玩"，但没说"手机屏幕小，布局要重新设计"。

---

## 输出格式

扫描完成后，输出三部分：

### 📋 技术零件清单
```
✅ 前端框架：React/Vue/纯HTML（用户已提）
✅ 托管平台：Vercel（用户已提）
⚠️ 实时数据库：Supabase Realtime（盲区——多人投票需要状态同步）
⚠️ 房间系统：需要生成+加入房间码（盲区）
❌ 支付系统：当前阶段不需要（不适用——首版随缘付费靠itch.io）
```

### 🔑 本次最关键盲区（Top 3）
按影响程度排，每个一句话解释为什么重要。

### 🛠️ 推荐技术选型（免费优先）
给出具体的技术栈建议，标注免费额度上限。

---

## 硬规则

1. **必须在写代码前运行。** 如果用户已经让你开始写代码了，先暂停，跑完本扫描再继续。
2. **不能跳过任何维度。** 每个维度都要输出判断，不能写"差不多就行"。
3. **可以判断"不适用"，但要说理由。** 不能说"❌ 不适用"就完了，必须写"当前阶段不需要，因为……"
4. **盲区标注要具体。** 不能写"可能需要后端"——要写"需要 Supabase Realtime，因为5个人同时投票需要状态同步"。
5. **推荐方案要能落地。** 不要推荐"可以用 AWS"——要写"Supabase 免费层，500MB 数据库，200 并发连接，你的场景完全够"。
